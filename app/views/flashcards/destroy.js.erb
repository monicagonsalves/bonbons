$('.delete-link').bind('ajax:success', function() {
   var card_id = $(this).attr('data-card-id')

   console.log('<%= Rails.application.routes.recognize_path(request.referrer) %>');

   $("#" + card_id + '_top').fadeOut(1000).detach();
});